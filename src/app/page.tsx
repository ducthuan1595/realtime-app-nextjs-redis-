import Button from "@/components/ui/Button";
import { db } from "@/lib/db";

export default async function Home() {

  interface UserMock {
    name: string
  }


  return (
    <Button variant={'ghost'}>THuan</Button>
  );
}
